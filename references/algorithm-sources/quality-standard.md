# 算法参考层质量标准（兼容入口）

本路径保留用于兼容早期链接，不再维护独立规则。

算法参考层唯一的当前质量标准为：

[`QUALITY_STANDARD.md`](QUALITY_STANDARD.md)

所有 `references/algorithm-sources/`、算法卡、`代码库/` README 与 study-only 模板都应以该文件为准。若本文件与 `QUALITY_STANDARD.md` 出现任何差异，始终以 `QUALITY_STANDARD.md` 为唯一权威版本。
