# Legacy demo paper

This directory is retained for backward-compatible root-level demos and
regression fixtures. It is not a formal competition project. New papers must
be created under `projects/<competition>/<year>/paper/` and routed with
`scripts/workflow.ps1 -Project ...`.
