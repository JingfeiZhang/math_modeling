# Workbench output

Operational reports and release files remain at this directory root. Historical
verification, demo, and temporary artifacts are organized under `_verification/`,
`_demos/`, and `_archive/`. Formal project output belongs under
`projects/<competition>/<year>/output/`.
